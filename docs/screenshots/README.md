# Screenshots for the tool README

Two images the top-level README references:

- **`proof-web.png`** — the browser proof view showing the Velarium and
  Verdana side-by-side comparison. Captured with an Armenian pangram at
  48px headline / 25px body.

- **`width-audit-panel.png`** — the Glyphs.app Width Audit panel with
  the Italic master selected against `System · Verdana` (auto-paired to
  Verdana Italic), 8 T1 mismatches visible.

Drop the PNGs here with those exact filenames; the README references
them by relative path (`docs/screenshots/…`).
