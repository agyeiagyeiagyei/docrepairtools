# Screenshots for the tool README

Images the top-level README references — drop PNGs here with these exact
filenames; the README links them by relative path (`docs/screenshots/…`).

- **`proof-web.png`** — the browser proof view showing the Velarium and
  Verdana side-by-side comparison.

- **`width-audit-panel.png`** — the Width Audit panel:
  Bold Italic master against the `Config · bold italic → VERDANABI.TTF`
  pin, yellow filter, mismatch list with Target/Ref/Δ columns.

- **`slant-panel.png`** — the Slant Glyphs panel with the live preview
  pane (slanted glyph in blue over the reference in red, extrema dots).

- **`coverage-panel.png`** — the Coverage panel with the searchable
  missing-glyph list (reference dropdown + Missing here / in ref / Both
  toggle visible).

- **`make-proof-panel.png`** — the Proof Builder panel (colour filter +
  proof pipeline launch/stop with log tail).

- **`coverage-report.png`** — the markdown coverage report (or a copied
  `.fea` file) showing gap tables and the feature-matching table.
