"""Glyphs.app control panel for `glyph-audit proof`.

The actual `Script → Glyph Proof` entrypoint lives in `panel.py`.
`glyph-audit proof panel install` symlinks it into Glyphs's user-scripts
directory so users don't have to know where the file physically lives.
"""
