"""Packaged Vite bundle for `glyph-audit proof serve`.

Source lives under `src/` and gets built into `dist/` at package-build
time. Only `dist/` ships in the wheel — see `pyproject.toml` package-data.
"""
